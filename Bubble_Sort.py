def bubble_sort(items, search_item):
    n = len(items)
    swapped = True # Initially set swapped to True
    while n > 0 and swapped:
        swapped = False #Reset at start of each iteration
        n = n - 1
        pass